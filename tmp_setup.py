from setuptools import setup, find_packages

# tmp file for testing etc.

setup(name='syndatagenerators',
      version='0.1',
      description='todo',
      packages=find_packages(),
      zip_safe=False)